{
  "name": "SWatch-shop-mock",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 5000 --host 0.0.0.0 --static ."
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
